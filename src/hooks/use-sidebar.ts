/**
 * Hook for accessing sidebar collapse state
 * Re-exports useSidebar from SidebarContext with proper error handling
 */

export { useSidebar } from '@/contexts/SidebarContext';

// Type re-export for convenience
export type { SidebarContextType } from '@/contexts/SidebarContext';
